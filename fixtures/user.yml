App\Entity\User:
  admin:
    email: admin@email.fr
    roles: ['ROLE_ADMIN']
    password: '<encodePassword(admin)>'
    enable: true

  user:
    email: user@email.fr
    roles: ['ROLE_ADMIN']
    password: '<encodePassword(user)>'
    enable: true

