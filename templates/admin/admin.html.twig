{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="section">

        <div class="row">
            <div class="col m12">
            <h1>Admin page !</h1>

                Liste des utilisateurs :
                <ul>
                    {% for user in users %}
                        <li>{{ user.email }}</li>
                    {% endfor %}

                </ul>

            </div>
        </div>
    </div>
</div>
{% endblock %}
